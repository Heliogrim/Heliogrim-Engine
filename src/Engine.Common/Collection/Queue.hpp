#pragma once

#include "../__macro.hpp"

#include <queue>

namespace hg {
    template <class Type_>
    using queue = std::queue<Type_>;

    template <class Type_>
    using Queue = std::queue<Type_>;
}
