#pragma once

namespace ember::engine::resource {
    class Package;
    struct PackageHeader;
    struct PackageIndex;
    struct PackageIndexEntry;
    struct PackageMeta;
    struct PackageFooter;
}
