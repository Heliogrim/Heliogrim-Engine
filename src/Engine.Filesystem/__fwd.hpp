#pragma once

namespace hg::engine::fs {
    //class File;
    class Path;
    class Url;
}

namespace hg::fs {
    //using File = ::hg::engine::fs::File;
    using Path = ::hg::engine::fs::Path;
    using Url = ::hg::engine::fs::Url;
}
